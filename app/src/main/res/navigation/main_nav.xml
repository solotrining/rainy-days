<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_nav"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.usan.feature.home.HomeFragment"
        android:label="HomeFragment" />
    <dialog
        android:id="@+id/alarmDialog"
        android:name="com.example.usan.feature.home.alarm.AlarmDialog"
        android:label="AlarmDialog" />
    <action
        android:id="@+id/action_global_alarmDialog"
        app:destination="@id/alarmDialog"
        app:popUpTo="@id/homeFragment" />
</navigation>